import { StyledNotFound } from "./style";

const NotFound = () => {
	return (
		<StyledNotFound>
			<h2>Não encontramos nenhum produto que corresponde a sua pesquisa :/</h2>
		</StyledNotFound>
	);
};

export default NotFound;
